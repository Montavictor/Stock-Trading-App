<h1>My Portfolio</h1>

<table class="table-users">
  <thead>
    <tr>
      <th>Company Name</th>
      <th>Quantity</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% if @stocks.any? %>
      <% @stocks.each do |stock| %>
        <tr>
          <td><%= stock.company_name %></td>
          <td><%= stock.quantity %></td>
          <td>
            <%= link_to "Sell", stock_path(stock, symbol: stock.company_name)%>
          </td> 
        </tr>
      <% end %>
    <% else %>
      <tr>
        <td colspan="8" class="text-center py-4 text-gray-600">No stocks yet</td>
      </tr>
    <% end %>      
  </tbody>
</table>

<%= link_to "Back to dashboard", transactions_path %>